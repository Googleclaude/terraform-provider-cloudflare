# Skill: cloudflare-ops

Skill compatível com Agent Skills standard. Funciona em Claude Code,
OpenCode e qualquer cliente que respeite o padrão.

## Instalação

### Claude Code
```bash
cp -r skills/cloudflare-ops ~/.claude/skills/
```

### OpenCode / Cursor
Veja documentação específica do cliente.

## Pré-requisitos
- MCP `ops-mcp` configurado e autenticado
- Opcionalmente: MCP oficial Cloudflare conectado para Code Mode
